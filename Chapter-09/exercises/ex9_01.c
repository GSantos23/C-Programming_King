/*
The following function, which computes the area of a tringle, contains two errors.
Locate the errors and show how to fix them. Hint: There are no error in the formula.

double triangle_area(double base, height)
double product;
{
    product = base * height;
    return product / 2;
}

*/

double triangle_area(double base, double height)
{
    double product;
    product = base * height;
    return (product / 2);
}