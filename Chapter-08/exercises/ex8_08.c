/**
 * Exercise 8.8
 * Write a declaration for a two-dimensional array named temperature_readings
 * that stores on month of hourly temperature readings. (For simplicity, assume
 * that a month has 30 days/). The rows of the array represent days of the month;
 * the columns should represent hours of the day
 */

#include <stdio.h>

int main()
{
	double temperature_readings[30][24];
}